<Window x:Class="P2P_projekt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="P2P Bank Node Monitor" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0" ResizeMode="CanResize">
    <Grid>
        <Border Background="White" Margin="40" CornerRadius="15" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.15" ShadowDepth="5"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="BANK NODE STATUS" Foreground="#777" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>

                <TextBlock Text="● ONLINE - RUNNING" Foreground="#2E7D32" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,30"/>

                <Grid Margin="0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="Total Funds (USD)" Foreground="Gray" FontSize="18"/>
                        <TextBlock x:Name="TxtFunds" Text="$0" FontSize="40" FontWeight="ExtraBold" Foreground="#333" Margin="0,5,0,0"/>
                    </StackPanel>

                    <Rectangle Grid.Column="1" Width="2" Fill="#EEE" Margin="20,0"/>

                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="Active Clients" Foreground="Gray" FontSize="18"/>
                        <TextBlock x:Name="TxtClients" Text="0" FontSize="40" FontWeight="ExtraBold" Foreground="#333" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>

                <Border Background="#F5F5F5" CornerRadius="8" Padding="15" Margin="0,30,0,10" HorizontalAlignment="Center">
                    <TextBlock x:Name="TxtIp" Text="IP: Detecting network..." FontSize="20" Foreground="#333" FontWeight="DemiBold"/>
                </Border>

                <Button x:Name="BtnStop" Content="SAFE SHUTDOWN" Background="#D32F2F" Foreground="White" 
                        Padding="20,15" Margin="0,20,0,0" FontWeight="Bold" FontSize="16" 
                        BorderThickness="0" Cursor="Hand" HorizontalAlignment="Center" Width="220"
                        Click="BtnStop_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>